apiVersion: v1
kind: Service
metadata:
  name: mvp-oficina-api-service
  namespace: staging
  labels:
    app: mvp-oficina-api
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: mvp-oficina-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000